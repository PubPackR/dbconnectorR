% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_extern_event_classification.R
\name{identify_real_no_show_reschedules}
\alias{identify_real_no_show_reschedules}
\title{Identify Genuine No-Shows Among Reschedule-Excluded Events}
\usage{
identify_real_no_show_reschedules(
  reschedule_event_ids,
  event_call_times,
  event_participant_emails,
  slot_window_minutes = 30
)
}
\arguments{
\item{reschedule_event_ids}{Integer vector of event_ids currently flagged for
reschedule-exclusion.}

\item{event_call_times}{Data frame with one row per event and columns
\code{event_id}, \code{call_start}, \code{event_start}, \code{event_updated_at} (the deduped
mapped call plus event timing).}

\item{event_participant_emails}{Data frame with columns \code{event_id}, \code{email}
for all participants of the events in scope.}

\item{slot_window_minutes}{Numeric. Maximum absolute distance (minutes)
between \code{call_start} and \code{event_start} to count as "call at the slot".
Default 30.}
}
\value{
Integer vector: the subset of \code{reschedule_event_ids} that are genuine
no-shows and should stay counted (not excluded).
}
\description{
A reschedule exclusion (\code{verschoben} / \code{rescheduled_without_meeting_id})
removes the canceled leg of a moved meeting. That is correct when a meeting
was moved \emph{before} it happened, but wrong when the lead no-showed and the
meeting was only rebooked \emph{afterwards} -- per definition such a no-show still
counts. This helper returns the subset of reschedule-excluded event_ids that
are genuine no-shows and must therefore NOT be excluded.
}
\details{
An event qualifies when ALL hold:
\itemize{
\item an external lead was invited: at least one participant email that is
neither internal (\code{is_internal_email}) nor synthetic (\code{is_synthetic_email})
-- rules out internal meetings mis-tagged as \code{extern_planned};
\item an internal call actually took place at the scheduled slot: the event's
(deduped) mapped \code{call_start} lies within \code{slot_window_minutes} of
\code{event_start} -- positive evidence the meeting time arrived and someone
internal joined; and
\item the event was cancelled/last modified AFTER the slot
(\code{event_updated_at > event_start}) -- the no-show happened before the
reschedule. This rules out meetings moved \emph{before} their time (proactive
reschedule, no no-show), where a call may still map to the slot via a
shared recurring meeting id. \code{event_updated_at} is the same
cancellation-time proxy the pipeline already uses for \code{is_short_lived_event}.
}
}
\keyword{internal}
