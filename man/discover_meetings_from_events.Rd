% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_scoped_calls.R
\name{discover_meetings_from_events}
\alias{discover_meetings_from_events}
\title{Meeting-Discovery aus den delegiert ingestierten Kalender-Events (DB, kein Graph)}
\usage{
discover_meetings_from_events(con, cfg)
}
\arguments{
\item{con}{DB-Pool.}

\item{cfg}{load_scoped_config(); \code{raw_schema} steuert das Quell-Schema,
\code{events_days_back} das Fenster (nur vergangene/laufende Meetings),
\code{tenant_id} filtert auf Meetings des eigenen Tenants (Alt-Tenant-URLs
sind app-only unerreichbar).}
}
\value{
data.frame(join_url, organizer_oid), distinct.
}
\description{
Ersetzt das fruehere app-only calendarView-Lesen in der Discovery
(403 — \code{Calendars.Read} als Application-Permission wird nie granted):
die delegiert ingestierten Events liefern \code{join_url}; der Organizer wird
ueber \code{is_organizer} -> msgraph_contacts -> msgraph_users (Email-Match)
auf seine object_id aufgeloest. Nur intern organisierte Meetings sind
aufloesbar — extern organisierte deckt die CsApplicationAccessPolicy
ohnehin nicht.
}
\keyword{internal}
