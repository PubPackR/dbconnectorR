% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_events.R
\name{msgraph_update_booking_appointments}
\alias{msgraph_update_booking_appointments}
\title{Retrieve and Update Booking Appointments From MSGraph}
\usage{
msgraph_update_booking_appointments(
  con,
  access_token,
  startDate,
  suppression_pepper = NULL
)
}
\arguments{
\item{con}{A PostgreSQL database connection object.}

\item{access_token}{MSGraph API access token. Either a raw token string or
a token provider closure produced by \code{\link[=msgraph_make_token_provider]{msgraph_make_token_provider()}}. A
provider is auto-refreshed before expiry and on 401 responses.}

\item{startDate}{Date from which to retrieve appointments.}

\item{suppression_pepper}{Optional. Character pepper for DSGVO suppression.
If non-NULL, loads the suppression list via
\code{Billomatics::dsgvo_load_suppression()} and tombstones PII of deleted
persons in the booking participants before they are written to contacts and
event participants. Default \code{NULL} leaves behaviour unchanged.}
}
\value{
No return value. Updates database tables.
}
\description{
Retrieves Microsoft Bookings appointments for all booking businesses in the
tenant, converts them to the same shape as regular calendar events, and
writes them through the existing \code{update_events()},
\code{update_contacts_from_events()}, and \code{update_event_participants()} pipeline.
}
\details{
Externe Kunden (\code{customers} in der Bookings-API) erscheinen damit in
\code{raw.msgraph_event_participants} — was über die \code{calendarView}-API nicht
möglich ist, weil Kunden dort nicht im \code{attendees}-Feld stehen.

Match zu bestehenden Calendar-Events erfolgt über die Teams-\code{meeting_id};
wenn kein Match → neue Event-Row mit \code{msgraph_ical_uid = "booking:<id>"}.
}
\examples{
msgraph_update_booking_appointments(con, access_token, startDate)
}
