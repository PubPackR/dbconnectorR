% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_events.R
\name{coalesce_show_as}
\alias{coalesce_show_as}
\title{Coalesce a freshly-fetched show_as onto the previously stored value}
\usage{
coalesce_show_as(new_participants, existing_show_as, by)
}
\arguments{
\item{new_participants}{Data frame mit den Join-Key-Spalten (siehe \code{by})
plus \code{show_as} - die frisch aus Graph gefetchten Werte.}

\item{existing_show_as}{Data frame mit den Join-Key-Spalten plus
\code{show_as} - die aktuell in der DB gespeicherten Werte (z.B. gefiltert
auf \code{source == "calendar"}, damit ein Booking-Lauf, der \code{show_as} nie
kennt, keinen echten Kalender-Wert scheinbar bestaetigt).}

\item{by}{Character-Vektor der Join-Key-Spalten, z.B.
\code{c("contact_id", "event_id")}.}
}
\value{
\code{new_participants}, \code{show_as} ersetzt durch
\verb{coalesce(show_as, bestehender show_as)} fuer Zeilen mit einem Match in
\code{existing_show_as}, unveraendert sonst.
}
\description{
\code{show_as} ist eine normale Upsert-Spalte unter \code{match_cols}. Ohne diese
Funktion wuerde ein frischer \code{show_as = NA} (der Lookup fand diesmal
keinen Treffer - Kalenderfreigabe zwischenzeitlich entzogen, Person
ausgeschieden, transienter Graph-Fehler) einen zuvor korrekt gesetzten
Wert (z.B. \code{"oof"} aus dem D2-Backfill oder einem frueheren Lauf)
unbemerkt ueberschreiben (Review-Fund W6). \code{coalesce()} behebt das: ein
neuer \code{NA}-Wert behaelt den bestehenden Wert, nur ein neuer NICHT-\code{NA}-
Wert darf ueberschreiben (auch ein anderer als vorher - z.B. bei einem
echten Statuswechsel).
}
\keyword{internal}
