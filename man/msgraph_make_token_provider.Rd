% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_msgraph_func.R
\name{msgraph_make_token_provider}
\alias{msgraph_make_token_provider}
\title{Create a Self-Refreshing MS Graph Token Provider}
\usage{
msgraph_make_token_provider(
  tenant_id,
  client_id,
  client_secret,
  refresh_buffer_seconds = 300
)
}
\arguments{
\item{tenant_id}{Azure AD tenant ID.}

\item{client_id}{Application (client) ID.}

\item{client_secret}{App registration client secret.}

\item{refresh_buffer_seconds}{Refresh the cached token when fewer than this
many seconds remain until expiry. Default 300 (5 minutes).}
}
\value{
A function \verb{function(force_refresh = FALSE)} returning a valid access
token string. Call with \code{force_refresh = TRUE} to bypass the cache.
}
\description{
Returns a closure that lazily fetches an MS Graph access token and caches it
until shortly before its JWT \code{exp} claim, then transparently refreshes it.
The returned closure can be passed wherever the package expects an
\code{access_token} argument — \code{fetch_with_retry()} and the direct \code{httr::GET}
calls in the \verb{msgraph_update_*} functions resolve it on demand and also
force-refresh on a 401 response.
}
\examples{
\dontrun{
token_provider <- msgraph_make_token_provider(
  tenant_id     = "f34ad13c-...",
  client_id     = "128a9a89-...",
  client_secret = keys$msgraph
)
msgraph_update_events(con, token_provider, startDate = Sys.Date() - 50)
}
}
