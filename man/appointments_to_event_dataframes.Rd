% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_events.R
\name{appointments_to_event_dataframes}
\alias{appointments_to_event_dataframes}
\title{Convert Booking Appointments to Event + Participant Data Frames}
\usage{
appointments_to_event_dataframes(appointments, staff_lookup, existing_events)
}
\arguments{
\item{appointments}{List of raw appointment objects (from \code{retrieve_booking_appointments}).}

\item{staff_lookup}{Data frame produced by \code{build_staff_lookup()}.}

\item{existing_events}{Data frame from \code{dplyr::collect(dplyr::tbl(con, I("raw.msgraph_events")))}.}
}
\value{
Named list with two data frames: \code{events} (shape of \code{all_calendar_events_}) and \code{participants} (shape of \code{msgraph_event_participants}).
}
\description{
Maps raw booking appointments to the same dataframe shape that
\code{msgraph_update_events()} produces, so that the downstream
\code{update_events()} / \code{update_contacts_from_events()} /
\code{update_event_participants()} functions can be reused as-is.
}
\details{
Matching of an appointment to an existing calendar-event row happens via the
Teams meeting_id extracted from \code{joinWebUrl}. If a match is found the
existing \code{msgraph_ical_uid} + \code{event_start} are reused and the appointment
only enriches the participants. If no match exists a new event row is
created with \code{msgraph_ical_uid = paste0("booking:", appointment$id)}.

Customers without an email address get a synthetic address
\verb{<bookingId>-<idx>@external.guest}, analog to the guest-handling in
\code{msgraph_calls.R}.

\code{update_events()} calls \code{dplyr::select(-attendees, -organizer)} on its
input, so placeholder \code{NA} columns are added to keep the shape compatible.
}
\keyword{internal}
