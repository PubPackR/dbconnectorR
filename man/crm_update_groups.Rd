% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/crm_groups.R
\name{crm_update_groups}
\alias{crm_update_groups}
\title{Ensure all given CRM group_ids exist in raw.crm_groups and return the mapping}
\usage{
crm_update_groups(con, group_ids)
}
\arguments{
\item{con}{A DBI database connection.}

\item{group_ids}{Integer vector of CRM group_ids (NAs are dropped), or a
data.frame with a group_id column.}
}
\value{
A tibble with columns \code{id} and \code{group_id} covering all existing
entries in raw.crm_groups (including newly inserted stubs).
}
\description{
For every group_id that is not yet registered in raw.crm_groups a stub row
is inserted with placeholder name "UNKNOWN_<group_id>" and is_stub = TRUE.
Existing entries are never modified - group names are maintained by hand,
because CentralStation CRM has no groups API endpoint.
}
\details{
Returns the mapping \verb{(id, group_id)} for all groups so that callers can
resolve the CRM-side group_id to the internal surrogate id via
\code{resolve_group_id()} before upserting raw.crm_leads / raw.crm_companies.
}
