% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_extern_event_classification.R
\name{compute_original_created_at}
\alias{compute_original_created_at}
\title{Determine the Original Creation Date per iCal UID}
\usage{
compute_original_created_at(events)
}
\arguments{
\item{events}{Data frame with columns \code{msgraph_ical_uid}, \code{event_created_at}
(Graph's \code{createdDateTime}) and \code{created_at} (our first insert).}
}
\value{
Data frame with one row per \code{msgraph_ical_uid} and the column
\code{original_created_at}.
}
\description{
\code{original_created_at} is the anchor of every "Termine gelegt" figure: it says
in which month a meeting was scheduled. It used to be \code{min(event_created_at)}
per \code{msgraph_ical_uid}, taking Graph's \code{createdDateTime} at face value.
}
\details{
That broke in the August 2026 tenant migration. Graph started reporting a
\strong{new} \code{createdDateTime} for meetings that already existed. No second event
appeared -- the same row, same id, same uid, simply got a later stamp.
Measured on 2026-09-01, 1975 of 2768 post-cutover meetings carried a Graph
date that fell on average 68 days \emph{after} our own first ingest of that row.
Their scheduling work moved retroactively into August: the week of the 17th
showed 1713 meetings instead of 377, while June and July lost the same
amount.

The fix needs no pairing and no heuristic, because the correct answer is
already in our own data. \code{raw.msgraph_events.created_at} is the moment we
first inserted the row, and \code{Billomatics::postgres_upsert_data} excludes
\code{created_at} from its update columns, so it never moves. A meeting we already
held on 31 July cannot have been created on 19 August. The ingest stamp is
therefore a hard upper bound, at most one nightly run away from the truth,
and the earlier of the two dates wins.

Deliberately applied to all rows, not just the migration window: the same
pattern recurs with every further calendar share (see the note in base-62's
\code{config.yaml}), and a rule bound to fixed dates would not survive it. Series
occurrences were checked and are unaffected -- of the 484 meetings shifted by
more than 90 days, 416 are single instances with exactly one occurrence per
uid.

\code{event_created_at} is the reference representation, not UTC: the column is a
\verb{timestamp without time zone} holding UTC digits, which some drivers hand back
tagged with the session timezone. \code{created_at} is a genuine \code{timestamptz} and
is converted to those same digits before the comparison, so \code{pmin} compares
digits with digits.

\strong{\code{original_created_at} feeds more than one figure.} Moving it earlier also
moves these, all in shiny-99-modules:
\itemize{
\item \code{is_short_lived_event} (cancelled less than 24 h after creation) loses
events, because the distance to the cancellation grows. Both
\code{module_kpi_no_show} queries filter on that flag, so cancelled meetings
return to the no-show denominator; without a call they count as no-shows
and the rate rises.
\item The lead-time analysis in \code{module_kpi_no_show} (\code{get_lead_time_data}) is
defined as the distance between \code{original_created_at} and the meeting date.
It gets longer by construction, median and bucket distribution shift.
}

Both are consequences of the correction, not defects, but they change figures
that were signed off separately.

The result deliberately keeps \code{event_created_at}'s timezone attribute rather
than being tagged UTC. Callers keep working on raw driver values:
\code{is_short_lived_event} takes the difference to \code{event_updated_at}, and the
upsert writes into a column without a timezone. Tagging the result would shift
both by the local offset -- a meeting created 30 June 22:30 UTC would land as
1 July 00:30 in the table and count in the wrong month.
}
\keyword{internal}
