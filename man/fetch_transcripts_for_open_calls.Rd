% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_transcripts_load.R
\name{fetch_transcripts_for_open_calls}
\alias{fetch_transcripts_for_open_calls}
\title{Fetch transcripts for calls that pass 1 left without content}
\usage{
fetch_transcripts_for_open_calls(
  con,
  authentication_msgraph,
  users,
  calls,
  call_event_mapping,
  start_date,
  end_date,
  handled_call_ids = integer(0),
  max_calls = 150
)
}
\arguments{
\item{con}{Database connection}

\item{authentication_msgraph}{List with tenant_id, client_id, access_token}

\item{users}{Data frame of internal users (already loaded)}

\item{calls}{Data frame of calls (already loaded)}

\item{call_event_mapping}{Data frame of call-event mappings (already loaded)}

\item{start_date}{Start of the load window (POSIXct)}

\item{end_date}{End of the load window (POSIXct)}

\item{handled_call_ids}{Call ids that pass 1 already produced a row for}

\item{max_calls}{Upper bound per run; a truncation is logged, never silent}
}
\value{
Tibble in the same shape as get_transcript_row(), possibly empty
}
\description{
Walks the external calls of the load window that still have no transcript
content, builds the meeting id from organiser and thread id, and reads the
transcript from the meeting-scoped endpoint.
}
\details{
Attribution is by time window — same meeting, transcript created strictly
between call start and end, exactly as get_call_id() does it. One Teams
meeting can produce several call records; the window is what separates them.
Anything ambiguous is left alone rather than guessed.
}
\keyword{internal}
