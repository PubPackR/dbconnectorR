% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_scoped_events.R
\name{mark_join_url_checked}
\alias{mark_join_url_checked}
\title{Geprueft-Marke \code{join_url_checked_at} nachtragen}
\usage{
mark_join_url_checked(con, rs, events)
}
\arguments{
\item{con}{Pool oder DBI-Verbindung.}

\item{rs}{Ziel-Schema (config-Schalter, i.d.R. "raw").}

\item{events}{\code{parse_scoped_events()$events}, also die gerade geschriebenen Zeilen.}
}
\value{
invisible(Anzahl markierter Zeilen).
}
\description{
Setzt die Marke auf genau den Zeilen, die Graph in diesem Lauf geliefert hat,
und nur dort, wo sie noch leer ist.
}
\details{
\strong{Warum ueberhaupt eine Marke.} Ein leeres \code{join_url} hat zwei voellig
verschiedene Bedeutungen: "kein Teams-Termin" und "nie danach gefragt". Nur
die erste ist eine Aussage. \code{join_url_checked_at} trennt beide, und der
Producer in \code{sales_meetings_unified.R} haengt seine Regel daran.

\strong{Warum nur wo NULL.} Die Marke ist die ERSTE Pruefung und wird nicht
fortgeschrieben. Wuerde der Ingest sie jede Nacht neu setzen, waere jede Zeile
im 50-Tage-Fenster \verb{IS DISTINCT FROM OLD}, der Trigger
\code{trigger_set_updated_at} zoege \code{updated_at} mit, und \code{updated_at} verloere
seine Bedeutung fuer den gesamten Bestand.

\strong{Warum ueber die gesehenen Zeilen statt ueber das Zeitfenster.} Eine Zeile,
die Graph nicht geliefert hat - etwa aus einem inzwischen nicht mehr
freigegebenen Kalender -, waere sonst faelschlich als geprueft ausgewiesen.
Genau diese stille Falschaussage soll die Marke ja verhindern.
}
\keyword{internal}
