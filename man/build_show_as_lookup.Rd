% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/msgraph_events.R
\name{build_show_as_lookup}
\alias{build_show_as_lookup}
\title{Build a (event_id, event_start, email) -> showAs Lookup}
\usage{
build_show_as_lookup(all_calendar_events_, users)
}
\arguments{
\item{all_calendar_events_}{Data frame mit mindestens \code{iCalUId},
\code{start_dateTime}, \code{user_id}, \code{showAs} - eine Zeile pro (Event,
Kalender-Kopie), wie von \code{msgraph_update_events()} aus der Graph-API
gebaut.}

\item{users}{Data frame mit mindestens \code{id} (interner User, matcht
\code{user_id} oben) und \code{email}.}
}
\value{
Data frame mit \code{event_id} (character), \code{event_start} (POSIXct),
\code{attendees_emailAddress_address} (lowercased email), \code{show_as}. Eine
Zeile pro (Event, Kalender-Kopie) mit auflösbarer E-Mail; Kopien ohne
internen User-Match werden verworfen (kein \code{show_as} zuweisbar - z.B.
fehlende Kalenderfreigabe oder ausgeschiedene Person, erwarteter
dauerhafter Zustand, kein Fehlerfall).
}
\description{
Jede Kalender-Kopie eines internen Users traegt ihr eigenes \code{showAs}
(oof/free/busy/...) - \code{msgraph_update_events()} laedt pro internem User
eine eigene \code{calendarView()}-Kopie, eine Zeile pro (Event,
Kalender-Kopie). Diese Funktion baut daraus die Zuordnung, die gebraucht
wird, um \code{show_as} pro Teilnehmer in \code{msgraph_event_participants} zu
annotieren.
}
\keyword{internal}
